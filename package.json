{
  "main": "server.js",
  "scripts": {
    "build": "spago build && spago bundle-app --to app/app.js",
    "serve": "node server.js",
    "launch": "npm run build && npm run serve"
  },
  "devDependencies": {
    "esbuild": "^0.17.10",
    "express": "^4.18.2"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}